@model ParentsPortal.Models.Payment

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Payment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StudentCourseId" class="control-label"></label>
                <select asp-for="StudentCourseId" class ="form-control" asp-items="ViewBag.StudentCourseId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SemesterFee" class="control-label"></label>
                <input asp-for="SemesterFee" class="form-control" />
                <span asp-validation-for="SemesterFee" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
